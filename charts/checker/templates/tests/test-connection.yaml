apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upbots.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "upbots.name" . }}
    helm.sh/chart: {{ include "upbots.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "upbots.fullname" . }}:{{ .Values.front.service.port }}']
  restartPolicy: Never
